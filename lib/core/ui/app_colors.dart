import 'package:flutter/painting.dart' show Color;

/// The `AppColors` class is in charge of the application colors.
class AppColors {
  /// Color `green` for aplication
  static const Color green = Color(0xFF3EB285);

  /// Color `grey` for aplication
  static const Color grey = Color(0xFF9B98C5);

  /// Color `scaffoldColor` for aplication
  static const Color scaffoldColor = Color(0xFFFAFAFA);

  /// Color `requiredColor` for aplication
  static const Color requiredColor = Color(0xFFBF4392);

  /// Color `purple` for aplication
  static const Color purple = Color(0xFF5843E0);
}
